<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The only permission the app declares besides notification
         access. There is deliberately no INTERNET permission: nothing
         in SpendWise can reach the network, by construction. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Allows the source picker to enumerate normal launcher apps without QUERY_ALL_PACKAGES. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name="${applicationName}"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="false">
        <activity
            android:name="com.spendwise.app.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.spendwise.app.SpendWiseNotificationListenerService"
            android:exported="true"
            android:label="SpendWise notification access"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Draws its own picture from a plain SharedPreferences file the
             app writes to — never the ledger, never a permission, never a
             network call. exported="true" is what every AppWidgetProvider
             sample declares so the system can deliver
             ACTION_APPWIDGET_UPDATE to it; the receiver answers to no other
             action and calling it uninvited does nothing but ask it to
             redraw the same shape from the same stored numbers. -->
        <receiver
            android:name="com.spendwise.app.SpendWiseHomeWidgetProvider"
            android:exported="true"
            android:label="@string/home_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/spendwise_home_widget_info" />
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
